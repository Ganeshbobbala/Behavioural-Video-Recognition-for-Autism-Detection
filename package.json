{
  "name": "autism-detection-root",
  "version": "1.0.0",
  "description": "Root runner for Autism Detection project",
  "scripts": {
    "start": "npm start --prefix autism-detection/backend",
    "dev": "npm run dev --prefix autism-detection/backend",
    "install-all": "npm install --prefix autism-detection/backend"
  }
}
